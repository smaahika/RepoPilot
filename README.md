# RepoPilot
Constrained coding agent that accepts a local/public Git repository plus a clearly scoped task, creates an implementation plan, inspects relevant files, edits code, run tests and lint checks inside an isolated workspace, producing a reviewable patch with an execution report. 
